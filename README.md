# 🐙 Tentacles Bot

Многофункциональный Telegram-бот для работы с видео: склейка, скачивание и сжатие.

>  Бот — результат слияния нескольких моих отдельных ботов в единый продукт.

| | |
|---|---|
|**Первоначальные исходники**| [Original_sources.zip](https://raw.githubusercontent.com/kage-kao/video_tentacles/refs/heads/main/Original_sources.zip) |

## ✨ Возможности

### 🎬 Склейка видео
- Объединение нескольких видео в одно
- Добавление движущегося водяного знака
- Настройка размера и скорости водяного знака
- Автоматическая нормализация видео (разрешение, кодек, аудио)

### 📥 Скачивание видео
- Поддержка 1000+ сайтов (TikTok, Instagram, Twitter, VK и др.)
- Автоматическая загрузка на tempshare.su (хранение 3 дня)
- На основе yt-dlp

### 📦 Сжатие видео

**Вариант 1 — FFmpeg (локальное сжатие)**
- Сжатие до заданного размера в МБ
- Кодеки: H.264, H.265, AV1
- Гарантированное достижение целевого размера

**Вариант 2 — ezgif.com (облачное сжатие)**
- Выбор разрешения, битрейта и формата
- Поддержка MP4, WebM, MKV
- Максимальный размер файла: 200 МБ

## 🚀 Установка

### Требования
- Python 3.10+
- FFmpeg и FFprobe

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/tentacles_bot.git
cd tentacles_bot
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp .env.example .env
```
Отредактируйте `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

5. **Запустите бота:**
```bash
python bot.py
```

## 📋 Команды бота

### Основные
| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/help` | Справка по командам |
| `/cancel` | Отмена текущей операции |

### Склейка видео
| Команда | Описание |
|---------|----------|
| `/merge` | Начать режим склейки |
| `/merge_now` | Склеить загруженные видео |
| `/merge_status` | Статус очереди |
| `/merge_clear` | Очистить очередь |
| `/watermark` | Установить водяной знак |
| `/size <1-50>` | Размер водяного знака (% высоты) |
| `/speed <10-200>` | Скорость движения (px/сек) |

### Скачивание
| Команда | Описание |
|---------|----------|
| `/download` | Скачать видео по ссылке |

### Сжатие
| Команда | Описание |
|---------|----------|
| `/compress` | Меню сжатия |
| `/compress <url> <mb> [codec]` | Быстрое сжатие |

**Примеры:**
```
/compress https://example.com/video.mp4 50
/compress https://example.com/video.mp4 100 h265
/compress https://example.com/video.mp4 25 av1
```

## 🛠 Технический стек

- **Telegram API:** aiogram 3.x
- **Видеообработка:** FFmpeg, FFprobe
- **Скачивание:** yt-dlp
- **Облачное сжатие:** ezgif.com API
- **Хранение файлов:** tempshare.su
- **HTTP-клиенты:** aiohttp, httpx, requests

## 📁 Структура проекта

```
tentacles_bot/
├── bot.py           # Основной файл бота
├── compressor.py    # Модуль сжатия видео (FFmpeg)
├── requirements.txt # Зависимости Python
├── .env             # Конфигурация (токен бота)
└── README.md        # Документация
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | ✅ |

### Настройки в коде

```python
# Лимиты
MAX_CONCURRENT_JOBS = 1        # Макс. одновременных задач сжатия
MAX_DOWNLOAD_SIZE_MB = 1024    # Макс. размер скачиваемого файла

# Хранение файлов
TEMPSHARE_DURATION = 3         # Дней хранения (склейка/скачивание)
TEMPSHARE_EZGIF_DURATION = 7   # Дней хранения (ezgif сжатие)
```

## 📝 Примечания

- **Telegram Bot API ограничение:** файлы до 20 МБ для загрузки ботом
- **ezgif.com ограничение:** файлы до 200 МБ
- **AV1 кодек:** лучшее сжатие (на 30-50% эффективнее H.264), но медленнее

## 📄 Лицензия

- **MIT License**

## 🤝 Разработано:

- **KageKao**
