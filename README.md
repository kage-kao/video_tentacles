# 🐙 Tentacles Bot

Многофункциональный Telegram-бот для работы с видео: склейка, скачивание и сжатие.

> Бот — результат слияния нескольких отдельных ботов в единый продукт.

| | |
|---|---|
| **Первоначальные исходники** | [Original_sources.zip](https://raw.githubusercontent.com/kage-kao/video_tentacles/refs/heads/main/Original_sources.zip) |

---

## ✨ Возможности

### 🎬 Склейка видео
- Объединение нескольких видео в одно
- Добавление движущегося водяного знака
- Настройка размера (1–50% высоты) и скорости (10–200 px/сек) водяного знака
- Автоматическая нормализация (разрешение 1280×720, кодек H.264, аудио AAC)

### 📥 Скачивание видео
- Поддержка 1000+ сайтов (TikTok, Instagram, Twitter, VK и др.)
- Автоматическая загрузка на tempshare.su (хранение 3 дня)
- На основе yt-dlp

### 📦 Сжатие видео

**Вариант 1 — FFmpeg (локальное)**
- Сжатие до заданного размера в МБ с гарантией результата
- Кодеки: H.264 (быстро), H.265 (лучше), AV1 (лучшее сжатие, −30–50%)
- До 3 попыток с автоподбором битрейта
- Быстрый запуск: `/compress <url> <мб> [h264|h265|av1]`

**Вариант 2 — ezgif.com (облачное)**
- Выбор разрешения, битрейта и формата (MP4 / WebM / MKV)
- **Автоматическое разбиение на части** для файлов > 190 МБ:
  - Видео нарезается на чанки по ~190 МБ
  - Каждый чанк сжимается через ezgif.com отдельно
  - Все части склеиваются обратно в одно видео
  - Поддержка видео практически любого размера (протестировано до 1+ ГБ)

---

## 🚀 Установка

### Требования
- Python 3.10+
- FFmpeg и FFprobe (`apt install ffmpeg` или `brew install ffmpeg`)

### Быстрый старт

```bash
git clone https://github.com/yourusername/tentacles_bot.git
cd tentacles_bot

python -m venv venv
source venv/bin/activate       # Linux/macOS
# venv\Scripts\activate        # Windows

pip install -r requirements.txt

echo "TELEGRAM_BOT_TOKEN=your_token_here" > .env

python bot.py
```

### Автозапуск через supervisor (Linux VDS)

Используйте готовый скрипт `start.sh` + конфиг `tentacles_bot.conf`.

`start.sh` при каждом запуске автоматически:
1. Проверяет наличие `ffmpeg` / `ffprobe` — устанавливает если отсутствуют
2. Проверяет Python-зависимости — устанавливает из `requirements.txt` если отсутствуют
3. Запускает бота

```bash
# Скопировать конфиг supervisor
cp tentacles_bot.conf /etc/supervisor/conf.d/

# Применить
supervisorctl reread && supervisorctl update
supervisorctl status tentacles_bot
```

---

## 📋 Команды бота

### Основные
| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/help` | Справка по всем командам |
| `/cancel` | Отмена текущей операции (включая активное сжатие) |
| `/status` | Статус текущей задачи сжатия |

### 🎬 Склейка видео
| Команда | Описание |
|---------|----------|
| `/merge` | Начать режим склейки |
| `/merge_now` | Склеить все загруженные видео |
| `/merge_status` | Статус очереди (количество видео, водяной знак) |
| `/merge_clear` | Очистить очередь видео |
| `/watermark` | Установить изображение водяного знака |
| `/size <1–50>` | Размер водяного знака в % от высоты видео |
| `/speed <10–200>` | Скорость движения водяного знака (px/сек) |

### 📥 Скачивание
| Команда | Описание |
|---------|----------|
| `/download` | Скачать видео по ссылке (1000+ сайтов) |

### 📦 Сжатие
| Команда | Описание |
|---------|----------|
| `/compress` | Меню выбора варианта сжатия |
| `/compress <url> <мб> [кодек]` | Быстрый запуск FFmpeg-сжатия |

**Примеры быстрого сжатия:**
```
/compress https://example.com/video.mp4 50
/compress https://example.com/video.mp4 100 h265
/compress https://example.com/video.mp4 25 av1
```

---

## 🔄 Как работает ezgif с большими файлами

```
Видео 1000 МБ
    │
    ├─ HEAD-запрос → определяем размер
    ├─ Скачиваем локально с прогрессом
    ├─ ffmpeg нарезает на чанки по 190 МБ
    │     ├─ chunk_000.mp4 (190 МБ)
    │     ├─ chunk_001.mp4 (190 МБ)
    │     ├─ chunk_002.mp4 (190 МБ)
    │     ├─ chunk_003.mp4 (190 МБ)
    │     └─ chunk_004.mp4 (240 МБ → ≈190 МБ после нарезки)
    │
    └─ Для каждого чанка:
          ├─ Загрузка на tempshare.su → получаем URL
          ├─ Отправка URL на ezgif.com
          ├─ Сжатие на серверах ezgif
          └─ Скачивание сжатого чанка
    │
    ├─ ffmpeg склеивает все сжатые чанки
    └─ Загрузка итогового файла на tempshare.su → ссылка пользователю
```

---

## 🛠 Технический стек

| Компонент | Технология |
|-----------|-----------|
| Telegram API | aiogram 3.x |
| Видеообработка | FFmpeg / FFprobe |
| Скачивание | yt-dlp |
| Облачное сжатие | ezgif.com |
| Хранение файлов | tempshare.su |
| HTTP-клиенты | aiohttp, httpx, requests |
| Парсинг HTML | BeautifulSoup4 |

---

## 📁 Структура проекта

```
tentacles_bot/
├── bot.py              # Основной файл бота (хендлеры, логика)
├── compressor.py       # Модуль FFmpeg-сжатия (побитрейтный, многопопытный)
├── start.sh            # Скрипт запуска с авто-установкой зависимостей
├── tentacles_bot.conf  # Конфиг supervisor для автозапуска
├── requirements.txt    # Python-зависимости
├── .env                # Переменные окружения (токен бота)
└── README.md           # Документация
```

---

## ⚙️ Конфигурация

### Переменные окружения (`.env`)

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | ✅ |

### Ключевые константы в `bot.py`

```python
MAX_CONCURRENT_JOBS    = 1     # Макс. одновременных задач сжатия на сервере
MAX_DOWNLOAD_SIZE_MB   = 1024  # Лимит скачивания для FFmpeg-сжатия (МБ)
TEMPSHARE_DURATION     = 3     # Хранение файлов склейки/скачивания (дней)
TEMPSHARE_EZGIF_DURATION = 7   # Хранение файлов ezgif (дней)
EZGIF_CHUNK_MB         = 190   # Размер одного чанка для ezgif (МБ)
```

---

## 📝 Ограничения

| Параметр | Лимит |
|----------|-------|
| Telegram Bot API (upload) | 20 МБ |
| FFmpeg-сжатие (скачивание) | 1024 МБ |
| ezgif.com (один файл) | 200 МБ |
| ezgif.com (с авто-чанкингом) | без ограничений* |

*ограничено только дисковым пространством сервера

---

## 🐛 Исправленные баги

- **`/cancel` не сбрасывал активную задачу сжатия** — после отмены бот говорил «уже есть активная задача». Теперь `/cancel` отменяет `asyncio.Task` и очищает все состояния.
- **ffmpeg не найден после перезапуска VDS** — `start.sh` автоматически устанавливает ffmpeg при каждом старте если он отсутствует.

---

## 📄 Лицензия

MIT License

## 🍄 Разработано

**KageKao 👤**
